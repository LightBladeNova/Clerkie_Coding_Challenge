"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

var operator = exports.operator = function operator(value, option) {
  if (Array.isArray(value)) {
    var arrayOperatorMap = {
      $eq: "$in",
      $ne: "$nin"
    };
    if (arrayOperatorMap[option]) {
      return _defineProperty({}, arrayOperatorMap[option], value);
    }
  }

  if (option === "$eq") {
    return value;
  }

  if (option) {
    return _defineProperty({}, option, value);
  }

  return value;
};
exports.default = {
  operator: operator
};